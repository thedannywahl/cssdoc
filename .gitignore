node_modules
dist
*.tsbuildinfo
*.vsix
# Anchored patterns match only the repo root, so match the nested docs/.vitepress too.
**/.vitepress/cache
**/.vitepress/dist
docs/reference
# Published to the docs site at build time from packages/config/cssdoc.schema.json.
docs/public/cssdoc.schema.json
# Vendored into the extension at build time from @cssdoc/tmlanguage.
servers/vscode/syntaxes
.DS_Store
