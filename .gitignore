node_modules
dist
*.tsbuildinfo
*.vsix
# Anchored patterns match only the repo root, so match the nested docs/.vitepress too.
**/.vitepress/cache
**/.vitepress/dist
docs/reference
# Vendored into the extension at build time from @cssdoc/tmlanguage.
servers/vscode/syntaxes
.DS_Store
